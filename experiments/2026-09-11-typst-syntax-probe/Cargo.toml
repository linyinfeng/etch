[package]
name = "typst-syntax-probe"
version = "0.0.0"
edition = "2024"
publish = false

# standalone: the repo root is a workspace for `lp` and must not claim this crate
[workspace]

[dependencies]
typst-syntax = "0.15"
