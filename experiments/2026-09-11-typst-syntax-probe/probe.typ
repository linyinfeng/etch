= Probe

- a list item with a nested chunk
  ```c
  int indented;
  ``` <in-list>

```py
x = 1
``` <same-line>

```py
y = 2
```
<own-line>

```py
z = 3
``` <dup>

```py
w = 4
``` <dup>

```text
not a chunk
```
