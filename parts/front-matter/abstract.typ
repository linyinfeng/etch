= Abstract

This book is a program, and the program is this book. It is literate in Knuth's sense — an essay whose code
is quoted in the order the argument needs rather than the order a compiler needs — and it is the source of
`lp`, the tool that tangles it into a Rust crate, a flake, a command line and the tests that check them, so
what gets edited, reviewed and diffed is the argument rather than a copy of it. What is offered is a position
and its demonstration — the argument first, the code as its by-product, and one program's evidence that the
position is livable — and nothing here checks the prose, the order of the argument or whether the argument is
any good: those cannot be checked today, so they are the maintainer's responsibility, human or machine.
