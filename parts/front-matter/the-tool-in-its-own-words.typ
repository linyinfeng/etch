= The tool, in its own words

The name is a verb from the printing half of this method: a plate is etched, and copies are pulled off it —
which is the relation this document has to the tree.

This is the whole of `etch`: the program and the package it is written with. There is no second source —
the crate is tangled out of this document, the package is one of the files it declares, and
`etch weave etch.typ etch.pdf` renders what you are reading: `typst compile` with a root that covers the document,
and the book attached to the page it renders.

It is a literate program, and that is not a remark about its formatting. The document is where the
thinking lives; the code is quoted into it as the evidence that makes the thinking checkable.

The mechanical facts a writer needs — the notation, where a chapter goes, what the checks are — are in
*Editing this book*, in the repository chapter, where an editor arrives with files in hand.
